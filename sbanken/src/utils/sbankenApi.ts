const version = 'v2';
export const baseUrl = 'https://publicapi.sbanken.no/apibeta/';
export const getAccountsUrl = `api/${version}/Accounts`;
export const getPaymentsUrl = `api/${version}/Payments/`; //AccountId at the end
export const getTransactionsUrl = `api/${version}/Transactions/`; //AccountId at the end
